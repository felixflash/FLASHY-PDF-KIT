# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.gradle-user/

# Logs
*.log
hs_err_pid*
replay_pid*

# IDEs
.vscode/
.idea/
*.iml
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Android build outputs
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/captures/
android/*.apk
android/*.aab

# Capacitor outputs
# www/
# Note: In some workflows you might want to commit www if it's the source.
# But usually it's the build output. Looking at your project, www/index.html is the source.
# So I will NOT ignore www for now, unless you have a build step (like React/Vue).
# Since you have index.html directly in www, we should keep it.

# Sensitive data
google-services.json
*.keystore
*.jks

# IDE/Agent Artifacts
.artifacts/
android/.artifacts/
